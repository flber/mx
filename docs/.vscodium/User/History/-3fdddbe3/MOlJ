#!/usr/bin/luajit

function lines(s)
  if s:sub(-1)~="\n" then s=s.."\n" end
  return s:gmatch("(.-)\n")
end

local input = io.stdin:read("*all")

for line in lines do
  if string.find(line, "{{date}}") then
    
  end
end