#!/usr/bin/luajit

-- function lines(s)
--   if s:sub(-1)~="\n" then s=s.."\n" end
--   return s:gmatch("(.-)\n")
-- end

Filename = io.stdin:read()
Input = io.stdin:read("*all")

if Input:find("{{date}}") then
else
  print(Input)
end
