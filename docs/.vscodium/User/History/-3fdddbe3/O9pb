#!/usr/bin/luajit

-- function lines(s)
--   if s:sub(-1)~="\n" then s=s.."\n" end
--   return s:gmatch("(.-)\n")
-- end

local input = io.stdin:read("*all")

if input:find("{{date}}") then
  
else
  print(input)
end
