#!/usr/bin/luajit

Filename = io.stdin:read()
Input = io.stdin:read("*all")

if Input:find("{{feed}}") then
  local command = "date -r " .. Filename .. " +%d%m%y"
  local f = io.popen(command)
  local last_modified = f:read()

  print(Input:gsub("{{feed}}", last_modified))
else
  print(Input)
end
